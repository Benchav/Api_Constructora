const { initialUsuarios } = require('./usuarios');
const { initialProyectos } = require('./proyectos');
const { initialInventarioObra } = require('./inventario');
const { initialEmpleados } = require('./empleados');
const { initialFinanzas } = require('./finanzas');
const { initialLicitaciones } = require('./licitaciones');
const { initialPlanos } = require('./planos');
const { initialReportesDiarios } = require('./reportesDiarios');
const { initialSolicitudesMateriales } = require('./solicitudesMateriales');
const { initialSolicitudesDinero } = require('./solicitudesDinero');
const { initialOrdenesCompra } = require('./ordenesCompra');
const { initialInspeccionesCalidad } = require('./inspeccionesCalidad');
const { initialIncidentesSeguridad } = require('./incidentesSeguridad');

module.exports = {
  initialUsuarios,
  initialProyectos,
  initialInventarioObra,
  initialEmpleados,
  initialFinanzas,
  initialLicitaciones,
  initialPlanos,
  initialReportesDiarios,
  initialSolicitudesMateriales,
  initialSolicitudesDinero,
  initialOrdenesCompra,
  initialInspeccionesCalidad,
  initialIncidentesSeguridad
};